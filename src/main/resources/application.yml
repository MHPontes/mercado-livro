spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/mercadolivro?createDatabaseIfNotExist=true
    username: root
    password: root@2507
jackson:
  default-property-inclusion: non_null   # Não exibe campos nulos, ex no ControllerAdvice, campo error